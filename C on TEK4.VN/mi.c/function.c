#include <stdio.h>
/*
// function definition
int getProduct(int number1, int number2) {
    _
}

int main() {
    // get integer input from the user
    int n1, n2;
    scanf("%d %d", &n1, &n2);
    // call the function with arguments n1 and n2
    // store the returned value in the result variable
    int result = ;
    printf("%d", result);
    return 0;
}
//  cbrt() để tìm căn bậc ba của nó
// sqrt() để tìm căn bậc 2 của nó
 */