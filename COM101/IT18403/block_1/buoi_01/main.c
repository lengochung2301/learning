#include <stdio.h> //  là thư viện tệp tiêu đề cho phép chúng tôi làm việc với các chức năng đầu vào và đầu ra
// return 0 kết thúc main()chức năng.
/* Ký tự xuống dòng ( \n) được gọi là một chuỗi thoát và nó buộc con trỏ thay đổi vị trí của nó về đầu dòng tiếp theo trên màn hình. 
Điều này dẫn đến một dòng mới.*/
/* CÁCH COMMENT :
chúng tôi sử dụng //cho các bình luận ngắn và /* */



int main() {
  printf("Hello World!");
  return 0;
}